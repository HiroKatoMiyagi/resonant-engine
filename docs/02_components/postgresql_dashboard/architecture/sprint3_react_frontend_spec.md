# Sprint 3: React フロントエンド仕様書

## 0. 概要

**目的**: Notion代替のWebダッシュボードUIを構築
**期間**: 5日間
**前提**: Sprint 1, 2 完了

---

## 1. Done Definition

### Tier 1: 必須
- [ ] React 18 + TypeScript + Viteセットアップ
- [ ] メッセージUI（Slack風チャット）
- [ ] 仕様書管理画面（Markdown対応）
- [ ] Intent一覧・詳細画面
- [ ] 通知システムUI
- [ ] FastAPI バックエンドとの接続
- [ ] レスポンシブデザイン
- [ ] Docker統合（frontend コンテナ）

### Tier 2: 品質
- [ ] Tailwind CSS によるモダンデザイン
- [ ] コンポーネントテスト 10件以上
- [ ] 状態管理（React Query/Zustand）
- [ ] リアルタイム更新（polling）
- [ ] エラー境界実装

---

## 2. 技術スタック

```yaml
frontend:
  framework: React 18
  language: TypeScript
  bundler: Vite
  styling: Tailwind CSS
  state: React Query + Zustand
  routing: React Router v6
  http: Axios
  markdown: react-markdown
  icons: Lucide React
```

---

## 3. ページ構成

### 3.1 メッセージUI (`/messages`)

```
┌────────────────────────────────────────┐
│ 🏠 Resonant Dashboard                  │
├────────────┬───────────────────────────┤
│            │ ┌─────────────────────┐   │
│ Sidebar    │ │ Message History     │   │
│            │ │ ┌─────────────────┐ │   │
│ • Messages │ │ │ User: Text      │ │   │
│ • Specs    │ │ │ Yuno: Response  │ │   │
│ • Intents  │ │ │ Kana: Response  │ │   │
│ • Notify   │ │ └─────────────────┘ │   │
│            │ │                     │   │
│            │ │ ┌───────────┬─────┐ │   │
│            │ │ │ Input Box │ 送信 │ │   │
│            │ │ └───────────┴─────┘ │   │
│            │ └─────────────────────┘   │
└────────────┴───────────────────────────┘
```

**機能**:
- メッセージ履歴表示（無限スクロール）
- 新規メッセージ送信
- メッセージタイプ別スタイリング
- リアルタイム更新（5秒間隔polling）

### 3.2 仕様書管理 (`/specifications`)

```
┌────────────────────────────────────────┐
│ 📄 Specifications                      │
├────────────────────────────────────────┤
│ [+ 新規作成]  [検索: ______]          │
├────────────────────────────────────────┤
│ ┌───────────────────────────────────┐  │
│ │ Title: API設計仕様書              │  │
│ │ Status: [draft] → [review]       │  │
│ │ Tags: #api #sprint2              │  │
│ │ Last Updated: 2025-11-17         │  │
│ │ [Edit] [Delete]                   │  │
│ └───────────────────────────────────┘  │
│ ┌───────────────────────────────────┐  │
│ │ ...                               │  │
│ └───────────────────────────────────┘  │
└────────────────────────────────────────┘
```

**機能**:
- 仕様書一覧（フィルタ・検索）
- Markdownエディタ
- ステータス管理
- タグ付け
- バージョン履歴（表示のみ）

### 3.3 Intent一覧 (`/intents`)

```
┌────────────────────────────────────────┐
│ 🎯 Intents                             │
├────────────────────────────────────────┤
│ Status: [All▼] Priority: [High▼]      │
├────────────────────────────────────────┤
│ ┌───────────────────────────────────┐  │
│ │ 🟡 PENDING | Priority: 8          │  │
│ │ "API設計レビューを依頼"          │  │
│ │ Created: 10:30                    │  │
│ └───────────────────────────────────┘  │
│ ┌───────────────────────────────────┐  │
│ │ ✅ COMPLETED | Priority: 5        │  │
│ │ "テストコード実装"               │  │
│ │ Processed: 09:45                  │  │
│ └───────────────────────────────────┘  │
└────────────────────────────────────────┘
```

**機能**:
- Intent一覧表示
- ステータスフィルタ
- 優先度ソート
- 詳細表示（結果JSON）
- 新規Intent作成

### 3.4 通知パネル

```
┌─────────────────────┐
│ 🔔 3 New           │
├─────────────────────┤
│ ✅ Intent処理完了   │
│ ⚠️ 仕様書レビュー待ち │
│ ℹ️ システム更新     │
└─────────────────────┘
```

**機能**:
- ベルアイコン＋バッジ
- ドロップダウン通知リスト
- 既読/未読管理
- タイプ別アイコン

---

## 4. APIクライアント

```typescript
// src/api/client.ts
import axios from 'axios';

const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL || 'http://localhost:8000/api',
  timeout: 10000,
});

export const messagesApi = {
  list: (params) => api.get('/messages', { params }),
  create: (data) => api.post('/messages', data),
  update: (id, data) => api.put(`/messages/${id}`, data),
  delete: (id) => api.delete(`/messages/${id}`),
};

// 同様に specs, intents, notifications
```

---

## 5. ディレクトリ構造

```
frontend/
├── src/
│   ├── api/                 # APIクライアント
│   ├── components/          # 再利用コンポーネント
│   │   ├── Layout/
│   │   ├── Messages/
│   │   ├── Specifications/
│   │   ├── Intents/
│   │   └── Notifications/
│   ├── pages/               # ページコンポーネント
│   ├── hooks/               # カスタムフック
│   ├── stores/              # 状態管理
│   ├── types/               # TypeScript型定義
│   └── utils/               # ユーティリティ
├── public/
├── Dockerfile
├── package.json
├── tailwind.config.js
├── vite.config.ts
└── tsconfig.json
```

---

## 6. Docker統合

```yaml
# docker-compose.yml に追加
frontend:
  build:
    context: ../frontend
    dockerfile: Dockerfile
  container_name: resonant_frontend
  ports:
    - "3000:80"
  depends_on:
    - backend
  networks:
    - resonant_network
```

---

## 7. 成功基準

- [ ] ローカルで全画面が動作
- [ ] CRUD操作が全て機能
- [ ] レスポンシブ対応（モバイル/デスクトップ）
- [ ] エラーハンドリング実装
- [ ] Notionを使わずにダッシュボード操作可能

---

**作成日**: 2025-11-17
**作成者**: Kana（外界翻訳層 / Claude Sonnet 4.5）
