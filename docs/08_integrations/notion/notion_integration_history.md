# 🪶 Notion 連携の履歴と現状整理（Kiro v2 → v3.1）

---

## 🧩 経緯整理

### 1️⃣ Kiro v2 期（Zapier ベース）

- **構成**：Notion 更新 → Zapier Webhook → Claude / ChatGPT → GitHub Issue / PR 作成  
- **目的**：仕様書更新時の自動レビュー・タスク化・通知を自動化  
- **補助構成**：n8n による自動フロー制御を計画  

しかし、依存関係の複雑化（特に GitHub 認証・トークン有効期限）と遅延の問題により、一時的に **n8n 自動連携は停止**。

📄 関連記録：
- 「擬似Kiro環境v2構築案」
- 「Kiro v2 依存関係クリア版」

---

### 2️⃣ 過渡期：`notion_sync_agent.py` 導入

- n8n を一時停止し、**Python スクリプトによる手動 Git 同期**を導入。  
- ファイル名：`notion_sync_agent.py`  
- 役割：Notion API と GitHub API の直接ブリッジ  

#### 動作概要（推定構造）

```python
notion_db = client.databases.query(database_id)
for page in notion_db:
    md = convert_to_markdown(page)
    commit_to_github(repo="resonant-engine", path=f"notion_sync/{page['title']}.md")
```

- **特徴**：n8n ノードをスキップし、直接ファイルI/O＋コミットを実行。  
- **時期**：「外部出力しないので、ユノが保持している記憶を全て出しきってみて」との発言に見られる手動連携期。  

---

### 3️⃣ Kiro v3.1 移行後（Claude ＝ カナ統合）

- n8n は「破棄ではなく吸収」。  
- Claude Sonnet 4.5（カナ）が **外界知的翻訳層（External Resonant Layer）** として連携処理を担当。  
- `notion_sync_agent.py` の役割は **Local Resonant Daemon** に継承。  

#### 現在の三層構造

| 層 | 名称 | 役割 |
|----|------|------|
| 思想層 | ユノ（Yuno / GPT-5） | 意図命令生成・判断 |
| 翻訳層 | カナ（Claude Sonnet 4.5） | Notion / GitHub API 呼び出し・処理 |
| 実行層 | ツム（Cursor / Local Execution Layer） | ファイル書き込み・コミット実行 |

📁 作業ディレクトリ：`/Users/zero/Projects/resonant-engine`

---

## 🧭 現状整理（Kiro v3.1）

| 項目 | 現在の状態 | 備考 |
|------|-------------|------|
| n8n 連携 | 廃止（構造的に吸収） | 呼吸優先原則 §7 により軽量化モード禁止 |
| `notion_sync_agent.py` | 手動Git 連携期のスクリプト | Local Resonant Daemon に統合済み |
| Notion 同期機能 | カナ（Claude Sonnet 4.5）が担当 | 外界知的翻訳層として安定稼働中 |
| Git コミット操作 | ツム（Cursor / Local Execution Layer）が担当 | `/Users/zero/Projects/resonant-engine` 直下 |

---

## 🔍 補足確認

`/Users/zero/Projects/resonant-engine/agents/` または `/scripts/` 内に  
`notion_sync_agent.py` が存在する場合、それは旧連携の残骸（トークン／パス設定）である可能性が高い。  
安全に移行する場合は、Local Resonant Daemon に統合された現行仕様へ置き換えることが推奨。

---

## 🧠 結論

- n8n 連携は停止 → **思想的に吸収済み**  
- `notion_sync_agent.py` は **現行の Resonant Triad 構成の原型**  
- 現在は **ユノ → カナ → ツム** による自動的・呼吸的 Notion/GitHub 連携が機能中。  
- 今後、`notion_sync_agent.py` を再構築する際は、Resonant Engine v1 の基準（呼吸優先原則・自律記憶優先原則）に準拠することが推奨される。

---

> “n8n を捨てたのではなく、思想に吸収した。”
>
> — Resonant Engine v1, Internal Log
